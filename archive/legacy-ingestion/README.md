# Legacy ingestion archive

Superseded Zoro/McMaster ingestion experiments retained for historical context.
They are not part of production, development, CI, or automatic deployment.

Executable tools fail closed unless `PARTSOURCE_ENABLE_LEGACY_INGESTION=1` is
set explicitly. That opt-in is for non-production experiments using sanctioned
sources only; it does not authorize either archived scrape plan.
